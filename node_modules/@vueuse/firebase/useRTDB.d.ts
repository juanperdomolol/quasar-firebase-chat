import firebase from 'firebase';
import { Ref } from 'vue-demi';

interface RTDBOptions {
    autoDispose?: boolean;
}
/**
 * Reactive Firebase Realtime Database binding.
 *
 * @param docRef
 * @param options
 */
declare function useRTDB<T = any>(docRef: firebase.database.Reference, options?: RTDBOptions): Ref<T | undefined>;

export { RTDBOptions, useRTDB };
